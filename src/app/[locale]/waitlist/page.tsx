import Waitlist from '@/components/waitlist/Waitlist'

// Pre-launch waiting-list page. Standalone like /assessment — carries its own
// nav + footer, sits outside the (main) route group.
export default function WaitlistPage() {
  return <Waitlist />
}
